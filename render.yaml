services:
  - type: web
    name: rakib-music-api
    env: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y python3 python3-pip ffmpeg
      pip3 install --user -U yt-dlp
      chmod +x /root/.local/bin/yt-dlp
      /root/.local/bin/yt-dlp --version
      npm install
    startCommand: node index.js
